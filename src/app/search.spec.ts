import { Search } from './search';

describe('Search', () => {
  it('should create an instance', () => {
    expect(newSearch()).toBeTruthy();
  });
});
function newSearch(): any {
  throw new Error('Function not implemented.');
}

